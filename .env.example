# =============================================================================
# TTD-DR (Test-Time Diffusion Deep Researcher) Configuration
# =============================================================================
# Copy this file to .env and fill in your actual API keys and settings
# 
# Required: At least one of OpenAI or Azure OpenAI must be configured
# Optional: Search APIs enhance research capabilities but are not required
# =============================================================================

# -----------------------------------------------------------------------------
# Azure OpenAI Configuration (Recommended)
# -----------------------------------------------------------------------------
# If you have Azure OpenAI, configure these settings
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_KEY=your-azure-openai-api-key
AZURE_OPENAI_DEPLOYMENT=gpt-4o-mini
AZURE_OPENAI_API_VERSION=2024-12-01-preview

# -----------------------------------------------------------------------------
# OpenAI Configuration (Alternative)
# -----------------------------------------------------------------------------
# If you're using regular OpenAI instead of Azure
# OPENAI_API_KEY=sk-your-openai-api-key-here
# OPENAI_MODEL=gpt-4o-mini

# -----------------------------------------------------------------------------
# Model Configuration
# -----------------------------------------------------------------------------
# Default model to use (matches your deployment name for Azure)
DEFAULT_MODEL=gpt-4o-mini

# -----------------------------------------------------------------------------
# Search API Configuration (Optional but Recommended)
# -----------------------------------------------------------------------------
# Tavily Search API - Provides high-quality search results
# Get your key at: https://tavily.com/
TAVILY_API_KEY=tvly-your-tavily-api-key

# Naver Search API - Useful for Korean and Asian content
# Get your keys at: https://developers.naver.com/docs/serviceapi/search/
NAVER_CLIENT_ID=your-naver-client-id
NAVER_CLIENT_SECRET=your-naver-client-secret

# -----------------------------------------------------------------------------
# TTD-DR Research Configuration (Optional)
# -----------------------------------------------------------------------------
# Maximum number of research iterations per query
MAX_ITERATIONS=10

# Maximum number of sources to collect per research session
MAX_SOURCES=20

# LangGraph recursion limit (should be higher than MAX_ITERATIONS)
RECURSION_LIMIT=50

# Search results to retrieve per knowledge gap
SEARCH_RESULTS_PER_GAP=3

# -----------------------------------------------------------------------------
# Advanced Configuration (Optional)
# -----------------------------------------------------------------------------
# Temperature for LLM responses (0.0 = deterministic, 1.0 = creative)
# TEMPERATURE=0.7

# Enable debug logging
# DEBUG=false

# Custom system prompt prefix
# SYSTEM_PROMPT_PREFIX="You are an expert researcher..."
